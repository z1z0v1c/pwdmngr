#include <stdio.h>
#include <sqlite3.h>

int main(void)
{
    // int option;

    // printf("Choose an option (1/2): \n");
    // printf("1. Login \n");
    // printf("2. Register \n");

    // scanf("%d", &option);
    // printf("Choosed %d", option);

    // sqlite3 *db;
    // char *zErrMsg = 0;
    // int rc;
    
    // rc = sqlite3_open("test.db", &db);

    // if (rc)
    // {
    //     fprintf(stderr, "Can't open database: %s\n", sqlite3_errmsg(db));
    //     return (0);
    // }
    // else
    // {
    //     fprintf(stderr, "Opened database successfully\n");
    // }

    // return 0;
}